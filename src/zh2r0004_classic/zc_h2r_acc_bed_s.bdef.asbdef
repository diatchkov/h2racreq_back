projection;
strict;
use draft;

define behavior for ZC_H2R_ACC_BED_S alias AccApartBedAll

{
  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
  use action SelectCustomizingTransptReq;

  use association _AccApartBed { create; with draft; }
}

define behavior for ZC_H2R_ACC_BED alias AccApartBed

{
  use update;
  use delete;

  use association _AccApartBedAll { with draft; }
}